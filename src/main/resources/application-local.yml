# Thymeleaf
spring:
  thymeleaf:
    cache: false
# Redis
  redis:
    host: 127.0.0.1
    port: 6379
# Tomcat AJP Connection
tomcat:
  ajp:
    enabled: true
    port: 8009

spplogin:
  # 空メール関連
  emptymail:
    domain: dev.msg.disney.co.jp
    session-coop-key:
      expire: 10
      expire-timeunit: MINUTES
  core-webapi:
    proxy:
      enable: false
    base-url: http://localhost
    port: 8989